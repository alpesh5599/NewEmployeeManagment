import { EnumKeyToValuePipe } from './enum-key-to-value.pipe';

describe('EnumKeyToValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumKeyToValuePipe();
    expect(pipe).toBeTruthy();
  });
});
